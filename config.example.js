// Cookie string for all your app.bokio.se cookies
const COOKIE = '';
// The id of your company, can be found in the url of you company
const COMPANY_ID = '';
// Where to save the files.
// Will create a folder in this file with the date of the backup
const OUTPUT_PATH = '';

module.exports = {
  COOKIE,
  COMPANY_ID,
  OUTPUT_PATH,
};
